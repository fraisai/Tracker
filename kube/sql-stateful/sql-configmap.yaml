apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql
  namespace: trackr-namespace
  labels:
    app: mysql
    app.kubernetes.io/name: mysql
data:
  primary.cnf: |
    [mysqld]
    log-bin=mysql-bin
  replica.cnf: |
    [mysqld]
    super-read-only
